# CLAUDE.md

Este archivo redirige a [`AGENTS.md`](AGENTS.md), la fuente única de reglas
para asistentes de IA en este repo (estándar [agents.md](https://agents.md)).

**Por qué este archivo existe:** Claude Code lee `CLAUDE.md` automáticamente
en la raíz. Lo mantenemos como puntero corto para que el contenido viva una
sola vez en `AGENTS.md` (universal) y no haya copias paralelas que se
desincronicen.

**Otros punteros equivalentes** (mismo contenido, lectura automática del
agente respectivo):

- `CLAUDE.md` → Claude Code
- `.cursorrules` → Cursor (no creado todavía, opt-in)
- `.github/copilot-instructions.md` → GitHub Copilot (no creado todavía, opt-in)
- `GEMINI.md` → Gemini CLI (no creado todavía, opt-in)

Si vas a editar reglas, **editá `AGENTS.md`** y no este archivo.
